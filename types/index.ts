export type { User, Product, Category, Lead, LeadNote, Review, BlogPost, SearchLog } from "@prisma/client";
export type { Role, ProductStatus, ProductTag, LeadStatus, LeadScore, LeadType, ReviewStatus } from "@prisma/client";
